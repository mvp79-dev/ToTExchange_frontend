export enum EDefaultValue {
  PAGINATION_PAGE = 1,
  PAGINATION_PAGE_SIZE = 16,
}

export enum EConfigKeys {
  sortBy = "sortBy",
  orderBy = "orderBy",
  page = "page",
  pageSize = "pageSize",
  categoryParentId = "categoryParentId_in",
  categoryChildrenId = "categoryId_in",
  filterFromPrice = "from",
  filterToPrice = "to",
  searchKeyword = "keyword",
}

export const UPLOAD_IMAGE_MAX_COUNT = 5;

export const ESTIMATE_GAS = 70_000;
